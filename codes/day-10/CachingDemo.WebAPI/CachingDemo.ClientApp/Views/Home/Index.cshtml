@model IEnumerable<Product>
@{
    ViewData["Title"] = "Home Page";
}


<div>
    <h2>@TempData["message"]</h2>
    <hr/>
    <ul>
        @foreach (var item in Model)
        {
            var data = $"{item.Name}{item.Price}";
            <li>@data</li>
        }
    </ul>
</div>
